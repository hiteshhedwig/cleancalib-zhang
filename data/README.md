# data/

Place real calibration datasets here once Milestone 13 is reached.

Suggested layout:

```
data/
  my_camera_2024/
    images/
      0001.png
      0002.png
      ...
    board.json    # rows, cols, square_size
```

This directory is intentionally empty in the repository. Files placed
here should be excluded from git (add to `.gitignore`).
